name: Deploy demo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Prepare Pages site
        run: |
          rm -rf site
          mkdir -p site
          # Static pages
          cp -f *.html site/

          # SEO files
          if [ -f robots.txt ]; then cp -f robots.txt site/robots.txt; fi
          if [ -f sitemap.xml ]; then cp -f sitemap.xml site/sitemap.xml; fi

          # Bundles
          cp -r dist site/dist

          # Static assets (favicons, images)
          cp -r assets site/assets

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
